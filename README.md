## 🤔 Why is this a plugin?

This configuration is set up as a plugin so I can:

- Include all configuration, mappings, and modules well organized
- Load it easily, by doing:

```lua
require('nvim-huda').setup()

